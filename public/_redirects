# Cloudflare Workers static-assets redirects.
#
# IMPORTANT: Workers Assets only allows RELATIVE (same-origin) targets here.
# Cross-domain redirects (hunterramps.au -> newcastleramps.com.au) CANNOT live in
# this file — they must be a zone-level Redirect Rule on the hunterramps.au
# zone in the Cloudflare dashboard (see README "Redirect hunterramps.au").
#
# Add any same-origin path redirects below, e.g.:
#   /old-path  /new-path  301
